1.git init

2.git add .

3.git commit -m "init"

4.git branch -M main

5.git remote add origin https://github.com/Sherajus-Salehin/E_Commerce_Backend_SpringBoot.git

6.git push -u origin main
<Authenticated using a 30 day token access in in GitHub Desktop>

7.touch README.md(FAILED IN BOTH CMD AND TERMINAL)

8.ni README.MD

9.(./mvnw spring-boot:run ->works correctly, shows whitelabel error as I didn't create any controller yet.)

